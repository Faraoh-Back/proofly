export const dynamic = "force-dynamic";
import EventsListContent from '@/components/ui/Events'; // Seu componente que lista os eventos

export default function EventsPageWrapper() {
  return (
    <EventsListContent />
  );
}